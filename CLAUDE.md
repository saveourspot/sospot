# CLAUDE.md — SOSpot 프로젝트 컨텍스트

> 이 파일은 Claude Code가 작업 시작 시 읽는 프로젝트 컨텍스트입니다.
> 최종 갱신: 2026-08-15 · 제출 마감: **2026-08-31 (잔여 17일)**

---

## 1. 프로젝트 개요

**SOSpot (Save Our Spot)** — 소상공인 상권 이상징후 탐지 서비스
2026년도 공공데이터 활용 공모전 (대전지역 공공데이터 활성화 협의체 · 대전권대학 산학협의체) 웹/앱 개발 분야 출품작.

### 해결하려는 문제

정책 담당자가 "어느 지역을 먼저 봐야 하는가"를 판단하려면 여러 분기·지역·업종을 수작업으로 교차 비교해야 한다. 특히 **전반적 경기 흐름에 따른 변화**와 **특정 지역만의 부진**을 구분하기 어렵다.

### 핵심 인사이트 (실데이터 검증 완료)

대전 총 점포수는 최근 분기에 **증가**했고(78,246 → 78,607 → 80,704), 74개 업종 중 53개가 증가세다. 반면 대전 체감 BSI는 계속 악화됐다(72.7 → 65.2 → 60.0).

> **총량만 보면 상권이 좋아 보이지만 현장 체감은 반대다. 따라서 "얼마나 줄었나"가 아니라 "전체가 오르는데 여기만 안 올랐나"를 봐야 한다.**

이것이 상대격차(Relative Gap) 방식을 채택한 근거이며, 서비스의 존재 이유다.

### 대표 탐지 사례 (시연용)

| 지역 | 업종 | 점포수 추이 | 지역 증감률 | 대전 전체 | 상대격차 |
|---|---|---|---|---|---|
| 중구 목동 | 음식 | 123 → 121 → 117 | −3.3% | **+3.0%** | **−6.3%p** |
| 동구 판암1동 | 과학·기술 | 20 → 18 → 12 | −33.3% | +8.2% | −41.6%p |
| 중구 대흥동 | 부동산 | 29 → 27 → 24 | −11.1% | +0.2% | −11.3%p |

목동 사례가 서비스 가치를 가장 잘 보여준다. 117개는 여전히 많고 감소폭도 작아 단순 조회로는 안 보이지만, 대전 전체가 성장하는 동안 혼자 줄었다.

---

## 2. 현재 상태 (2026-08-15)

### 완료

- 요구사항 정의서 v0.6, 사용자 요구사항서(URS) v1.2 작성
- 실데이터 3개 분기 + BSI 검증 및 지표 로직 확정
- 모노레포 구조, Spring Boot + PostgreSQL + Docker 환경 구축
- Flyway V1~V3 적용 → 테이블 8개 생성 완료 (**데이터는 비어 있음**)
- Vite + React + Leaflet 스캐폴딩, `/api` 프록시 설정
- Python venv 및 pipeline 모듈 구성
- GitHub 초기 커밋

### 미완 (최우선 순)

1. **행정동 경계 GeoJSON 확보** — 행정안전부. **8/16까지 결론 필요.** 미확보 시 좌표 기반 버블맵으로 전환
2. Python 전처리 파이프라인 (`pipeline/src/` 6개 스크립트)
3. 분석 API 5종
4. 프론트 F0/F1/F2
5. AI 질의응답
6. 배포 및 서비스 URL 확보

---

## 3. 남은 일정 (8/15 ~ 8/31)

| 구간 | 기간 | 산출물 | 이탈 시 조치 |
|---|---|---|---|
| P1 | 8/15~8/17 | 전처리·집계 파이프라인, DB 적재, **GeoJSON 확보** | GeoJSON 미확보 → 좌표 버블맵 전환 |
| P2 | 8/18~8/20 | 지표 계산 + 분석 API 5종 | — |
| P3 | 8/21~8/24 | F0 메인 · F1 지도 · F2 상세 | **여기까지가 제출 가능 최소선** |
| P4 | 8/25~8/27 | F6 AI 질의응답 (도구 3종) | 미완 시 F3 업종 분석으로 대체 |
| P5 | 8/28~8/29 | 배포(Docker), **서비스 URL 확보**, 버그 수정 | 압축 금지 |
| P6 | 8/30 | 데모 영상, 사업계획서 최종본, 제출 서류 | — |
| 예비 | 8/31 | 제출 | **마감일에 개발 금지** |

**신청서에 '서비스 URL' 칸이 있다. 배포까지가 제출물이다.**

### 범위 조정 (확정)

| 구분 | 항목 |
|---|---|
| 반드시 구현 | F0 메인, F1 지도, F2 상세, F6 AI(도구 3종) |
| 여유 시 | F3 업종 분석, F4 지역 비교 전용 화면 |
| 삭제 확정 | F5 검토 목록 |

---

## 4. 기술 스택 및 구조

| 레이어 | 기술 |
|---|---|
| Backend | Spring Boot 4.1.0 · Java 17 (Zulu) · Gradle Groovy · Spring Data JPA |
| DB | PostgreSQL 16 (Docker) · Flyway 12.4 |
| 파이프라인 | Python 3.11 · pandas (오프라인 배치) |
| Frontend | React · Vite 8 · **Leaflet** · Recharts · axios |
| AI | Tool Calling, `WebClient` REST 직접 호출 + Caffeine 캐싱 |
| 배포 | Docker Compose 단일 서버 |

```
sospot/                       # 실제 경로: C:\Users\moonl\development\sospot\sospot
├── backend/                  # Gradle 루트 (sospot-backend)
│   ├── src/main/java/org/example/sospot/
│   ├── src/main/resources/
│   │   ├── application.yml               # 커밋
│   │   ├── application-local.yml         # 커밋 안 함 (비밀값)
│   │   ├── application-local.yml.example # 커밋
│   │   └── db/migration/V1~V3__*.sql
│   └── http/analysis.http    # API 스펙 겸 테스트
├── frontend/                 # Vite + React (JavaScript)
├── pipeline/                 # Python 전처리
├── data/{raw,geo}/           # raw는 커밋 제외
└── docker-compose.yml
```

### 실행

```bash
docker compose up -d                 # DB (재부팅 후 매번)
cd backend && ./gradlew bootRun      # :8080
cd frontend && npm run dev           # :5173
```

프론트의 `/api` 요청은 Vite 프록시가 8080으로 전달한다. 백엔드에 CORS 설정 불필요.

---

## 5. 데이터 — 검증된 사실

### 활용 데이터 (최종 2종)

| ID | 데이터셋 | 용도 |
|---|---|---|
| D1 | 소상공인시장진흥공단_상가(상권)정보_대전 | 핵심 분석 소스 (202512 / 202603 / 202606) |
| D2 | 소상공인시장진흥공단_소상공인 경기동향(BSI) 현황 | 경기 맥락 지표 + 방법론 근거 |

> 전통시장 지원 현황·시장경영지원사업은 분석 축이 맞지 않아 제외. 업종코드 데이터셋은 데이터 기준일 이전이라 제외(D1에 코드·명이 모두 포함되어 불필요).

### D1 실측 프로파일

| 항목 | 202512 | 202603 | 202606 |
|---|---|---|---|
| 행 수 | 78,246 | 78,607 | 80,704 |
| 행정동 | 82 | 82 | 82 |
| 업종 대/중/소 | 10 / 74 / 246 | 동일 | 동일 |
| 결측 (행정동코드·좌표·업종코드) | 0 | 0 | 0 |

- 분기 간 **업종 코드셋 변화 없음**, **행정동 코드-명 매핑 변화 없음** → 시계열 비교 안전
- 좌표 전량 대전 범위 내 (위도 36.1965~36.4937, 경도 127.2491~127.5377)

### 실사용 컬럼 9개

`행정동코드` `행정동명` `시군구명` `상권업종대분류코드` `상권업종대분류명` `상권업종중분류코드` `상권업종중분류명` `경도` `위도`

`시도명`은 대전 파일이 단일값이라 불필요. `상가업소번호`는 DC-1 검증 리포트 전용.

### 데이터 제약 (설계에 반영 필수)

| ID | 제약 | 대응 |
|---|---|---|
| DC-1 | 상가업소번호 유지율 96.8% → **89.6%** 로 급락. 한 분기 8,149건 소멸/10,246건 신규는 실제 폐창업으로 보기 어려움 | **개별 점포 추적 금지. 집계 단위 비교만** |
| DC-2 | 영업 중 상가 스냅샷. 총 점포수는 오히려 증가 | '폐업률' 표현 금지, '점포 수 변화'로 통일 + "감소가 폐업을 의미하지 않음" 안내 |
| DC-3 | **BSI에 지역×업종 교차 없음.** 지역(17개 시도)과 업종(9개)이 각각 별도 축. "대전 음식점업 BSI"는 존재하지 않음 | 점수 계산 배제. 메인 요약 카드 + 상세 차트 배경으로만 |
| DC-4 | BSI 재고·고용·영업이익·원재료 항목 최근 9개월 결측 | `경기전반체감`, `대전체감`, 업종별 체감만 사용 |
| DC-5 | BSI 최신월(2026.06)은 전망만 존재, 체감 NaN | 직전 확정월 사용, 기준월 명시 |
| DC-6 | 행정동×중분류 5,067조합 중 점포수 중앙값 6개. 하한 20 적용 시 **79.7% 탈락** | 중분류는 드릴다운 전용, **지도 기본은 대분류** |
| DC-7 | 하한 10이면 상위 결과가 14→13→11 같은 소표본으로 채워짐 | **하한 20 확정** |
| DC-8 | 행정동 경계 GeoJSON 미포함 | 행정안전부 데이터 별도 확보 |

### 집계 단위 이원화 (핵심 결정)

| 레이어 | 단위 | 조합 수 | 하한 20 적용 후 | 행정동 커버리지 |
|---|---|---|---|---|
| 지도 기본 | 행정동 × **대분류**(10) | 807 | **604** | **82/82 (100%)** |
| 드릴다운 | 행정동 × **중분류**(74) | 5,067 | 1,029 | 부분 |

### BSI 분기 집계 (방법론 근거)

| 분기 | 대전 체감 BSI | 총 점포수 | 증감 |
|---|---|---|---|
| 2025Q4 | 72.7 | 78,246 | — |
| 2026Q1 | 65.2 | 78,607 | +0.46% |
| 2026Q2 | **60.0** | 80,704 | +2.67% |

> 2026Q2는 6월 체감값 결측으로 **4~5월 확정값 평균**. 화면에 기준월 명시.

BSI 업종 컬럼(전국): 제조업 · 소매업 · 음식점업 · 부동산중개업 · 전문기술사업 · 교육서비스업 · 스포츠및오락관련 · 수리업 · 개인서비스업. 상가 대분류 10개 중 7개만 매핑 가능(숙박·시설관리·임대·보건의료는 대응 없음). **참고용이며 지표 계산에 쓰지 않는다.**

---

## 6. 이상징후 탐지 로직 (확정)

### 조합 단위 지표

```
N(d,c,t) = 행정동 d, 업종 c, 분기 t의 점포 수

g   = (N_t − N_{t−1}) / N_{t−1}                                  # 지역 증감률
G   = (ΣN_대전(c,t) − ΣN_대전(c,t−1)) / ΣN_대전(c,t−1)            # 대전 전체 동일 업종
RD  = g − G                                                       # 상대격차 (핵심 지표)
CUM = (N_t − N_{t−2}) / N_{t−2}                                   # 누적 변화율
C   = 1 if g(t) < 0 and g(t−1) < 0 else 0                         # 연속감소
```

### 점수 및 등급

```
전제: N_{t−2} ≥ 20        # 미만은 sample_size_flag = 'LOW', 등급 산정 제외

P_rd  = 전체 조합 풀에서 (−RD)의 백분위 (0~100)
P_cum = 전체 조합 풀에서 (−CUM)의 백분위 (0~100)

Score = 0.5 × P_rd + 0.3 × P_cum + 0.2 × (C × 100)

등급: ≥80 중점검토 / 65~80 주의 / 50~65 관심 / <50 정상
```

RD에 가중치를 가장 크게 준 이유: 전체 경기 흐름과 지역 고유 부진을 구분하는 것이 서비스의 차별점. 단순 감소량 기준이면 기존 조회 서비스와 차이가 없다.

### 행정동 종합 등급

```
이상업종수(d) = 표본 하한 충족 + Score ≥ 50 인 대분류 업종 수

Raw(d) = 상위 min(3, 유효조합수)개 업종 Score 평균 × min(1.0, 0.6 + 0.1 × 이상업종수)
Pct(d) = 대전 82개 행정동 내 Raw의 백분위

등급: Pct ≥ 90 중점검토 / 70~90 주의 / 40~70 관심 / <40 정상
```

**왜 행정동 단위에서 백분위를 다시 계산하는가**: 조합 Score를 행정동으로 집계하면 분포가 중앙으로 압축되어 조합 임계값(80/65/50)을 그대로 쓸 수 없다. 서비스 목적이 **행정동 간 우선검토 순위 제공**이므로 백분위로 변환한다. 등급은 위험도의 절대값이 아니라 **대전 내 상대적 검토 우선순위**를 의미한다.

**유효 조합 수는 평균 7.4개(최소 2, 최대 10). 동구 대청동만 2개**이므로 상위 3개 고정 참조 시 오류. `min(3, 유효조합수)` 필수.

### 검증된 기대 결과 (2026.06, 대분류, 하한 20)

| 구분 | 값 |
|---|---|
| 대상 조합 | 604 / 807 |
| 조합 등급 분포 | 중점검토 32 · 주의 71 · 관심 119 · 정상 382 |
| **행정동 등급 분포** | **중점검토 9 · 주의 16 · 관심 25 · 정상 32** (합 82) |
| 행정동 상위 | 목동 · 대흥동 · 온천2동 · 복수동 · 둔산3동 · 삼성동 · 가양2동 · 유천2동 · 문창동 |

> 조합 단위 수치(32/71/119/382)와 행정동 단위 수치(9/16/25/32)를 **화면에서 섞어 쓰지 말 것.** 단위를 항상 명시한다.

### 백분위 모집단 검증

전체 조합 풀(A안) vs 업종별(B안) 비교 결과 순위 상관 0.960, Top10 8/10 일치. 소표본 업종(숙박 11조합)에서 B안이 불안정하므로 **A안 채택**.

---

## 7. DB 스키마 (Flyway 적용 완료)

```sql
dim_period(period_id CHAR(6) PK, year, quarter, base_date)
dim_dong(dong_code CHAR(8) PK, sigungu, dong_name, center_lat, center_lng)
dim_category(cat_code VARCHAR(6) PK, cat_name, parent_code FK, cat_level)  -- MAJOR / MIDDLE

fact_store_count(dong_code, cat_code, period_id, cat_level, store_count)
    PK(dong_code, cat_code, period_id)

fact_anomaly(dong_code, cat_code, period_id, cat_level, store_count,
             growth_rate, city_growth_rate, relative_gap, cum_change_rate,
             consecutive_decline, sample_size_flag, score, grade)
    PK(dong_code, cat_code, period_id)

fact_dong_score(dong_code, period_id, raw_score, pct_score, grade,
                anomaly_cat_count, valid_cat_count)
    PK(dong_code, period_id)

fact_bsi(period_month CHAR(7), metric_name, value)
    PK(period_month, metric_name)
```

BSI 원본은 72컬럼 wide 포맷 → `(연월, 지표명, 값)` long으로 변환 적재.

### 마이그레이션 규칙

- 경로 `backend/src/main/resources/db/migration/`, 파일명 언더스코어 **2개**
- **적용된 마이그레이션은 절대 수정 금지** (체크섬 불일치로 부팅 실패). 변경은 `V4__` 신규 파일
- SQL 파일에는 순수 SQL만. 주석은 `--`
- `hibernate.ddl-auto: validate` 고정. `update` 금지

---

## 8. 전처리 파이프라인 (P1 작업)

```
pipeline/src/
├── load.py         # CSV 로드(utf-8) + 9개 컬럼 추출 + 검증
├── dimensions.py   # dim_dong, dim_category, dim_period 생성
├── aggregate.py    # fact_store_count (MAJOR/MIDDLE 2단 집계)
├── metrics.py      # fact_anomaly, fact_dong_score
├── bsi.py          # BSI 로드(cp949!) + long 변환
└── report.py       # 검증 리포트
```

처리 순서: CSV 로드 → 대전 필터(이미 대전 파일) → 9컬럼 추출 → 업종/행정동 마스터 생성 → 코드셋 diff 검증 → 2단 집계 → 지표 계산 → 적재 → 검증 리포트

**인코딩**: 상가정보 `utf-8`, BSI **`cp949`**

### 검증 쿼리 (적재 후 필수)

```sql
-- 분기별 총 점포수: 78246 / 78607 / 80704
SELECT period_id, SUM(store_count) FROM fact_store_count
WHERE cat_level='MAJOR' GROUP BY period_id ORDER BY period_id;

-- 행정동 82
SELECT COUNT(*) FROM dim_dong;

-- 업종: MAJOR 10, MIDDLE 74
SELECT cat_level, COUNT(*) FROM dim_category GROUP BY cat_level;

-- 행정동 등급: 중점검토 9 / 주의 16 / 관심 25 / 정상 32
SELECT grade, COUNT(*) FROM fact_dong_score WHERE period_id='202606' GROUP BY grade;

-- 상위 이상징후: 판암1동 과학·기술 1위
SELECT d.dong_name, c.cat_name, a.store_count, a.relative_gap, a.score
FROM fact_anomaly a
JOIN dim_dong d ON d.dong_code=a.dong_code
JOIN dim_category c ON c.cat_code=a.cat_code
WHERE a.period_id='202606' AND a.cat_level='MAJOR' AND a.sample_size_flag='OK'
ORDER BY a.score DESC LIMIT 10;
```

**숫자가 다르면 파이프라인 버그다. 정답을 이미 알고 있다는 것이 이 프로젝트의 강점이다.**

---

## 9. 화면 및 기능 요구사항

### 사용자 플로우

```
[F0] 메인 → [F1] 이상징후 지도 → [F2] 행정동 상세
                                    ├→ [F3] 업종 분석
                                    └→ [F4] 지역 비교
※ [F6] AI 질의응답은 모든 화면에서 사이드 패널로 진입
```

### F0. 메인 (M)

| ID | 요구사항 |
|---|---|
| FR-F0-01 | 서비스 정의 + 기준 분기(2026년 2분기) 표시 |
| FR-F0-02 | 요약 카드 4종: 분석 행정동 수 / 중점검토·주의 지역 수 / 대전 전체 점포 증감률 / 대전 체감 BSI |
| FR-F0-03 | 우선검토 지역 TOP 5 (클릭 → F2) |
| FR-F0-04 | CTA "이상징후 지도 보기" |
| FR-F0-05 (S) | AI 질의 입력창 + 추천 질문 3개 |
| FR-F0-06 | 데이터 출처·기준일자 상시 표기 |
| FR-F0-07 (S) | 지표 산출 방식 설명 페이지 링크 |

### F1. 이상징후 지도 (M)

| ID | 요구사항 |
|---|---|
| FR-F1-01 | 82개 행정동 코로플레스, 4색 등급 표시 |
| FR-F1-02 | **기본 레이어는 대분류** |
| FR-F1-03 | 업종 필터 (대분류 10종) |
| FR-F1-04 (S) | 자치구 필터 (5개 구) |
| FR-F1-05 | 기준 분기 **2026.06 고정** — 화면에 비교 구간(2025.12~2026.06) 명시 |
| FR-F1-06 | 우측 등급순 리스트, 지도↔리스트 양방향 하이라이트 |
| FR-F1-07 | hover 툴팁 (동명, 등급, 이상 업종 수) |
| FR-F1-08 | 클릭 → F2 |
| FR-F1-09 | 범례 + "대전 내 상대적 위치" 설명 |

> **분기 선택 기능은 불가능하다.** 2026.03을 기준으로 쓰려면 2025.09 데이터가 필요한데 보유하지 않았다. 3개 분기로 완전한 지표를 산출할 수 있는 시점은 2026.06 하나뿐.

### F2. 행정동 상세 (M) — 서비스의 심장

| ID | 요구사항 |
|---|---|
| FR-F2-01 | 헤더: 행정동명·자치구·등급·점수·대전 내 순위 |
| FR-F2-02 | 판정 근거 요약문 자동 생성 |
| FR-F2-03 | 이상 업종 TOP 3 카드 (업종명, 3분기 추이, 지역 증감률, 대전 증감률, 상대격차) |
| FR-F2-04 | 지역 vs 대전 전체 추세 라인차트 + 배경에 분기별 대전 체감 BSI 밴드 |
| FR-F2-05 | 업종별 상대격차 발산형 바차트 |
| FR-F2-06 | 표본 부족 업종은 '판정 제외' 섹션에 분리 |
| FR-F2-07 | F3 / F4로 이동 |
| FR-F2-08 (S) | AI에 지역 컨텍스트 주입 |

### F3. 업종 분석 (전체 S/C)

진입 컨텍스트에 따라 2모드: 행정동 선택 시 해당 지역 세부 업종 분석, 업종만 선택 시 대전 전체 지역 분포 분석. 화면은 하나, 상단 배지로 모드 표시.

### F4. 지역 비교 (전용 화면 S/C)

비교 기능 자체는 AI 도구 `compareRegions`로 M 범위에서 제공.

### F6. AI 질의응답 (M)

| ID | 요구사항 |
|---|---|
| FR-F6-01 | 자연어 → 의도 분류 + 파라미터 추출 |
| FR-F6-02 | 분석 API 호출 후 **그 결과만으로** 답변 생성 |
| FR-F6-03 | 답변 + 근거 표/미니 차트 |
| FR-F6-04 | 데이터 없음 / 범위 밖 명시적 안내 |
| FR-F6-05 (S) | 답변 내 지역명 클릭 → F2 |
| FR-F6-09 (S) | 진행 단계 표시 |

### 공통

| ID | 요구사항 | 우선 |
|---|---|---|
| FR-C-01 | 모든 수치 옆 기준 분기 표기 | M |
| FR-C-02 | 데이터 출처·기준일자 상시 노출 | M |
| FR-C-03 | 로딩·빈 상태·오류 상태 화면 | M |
| FR-C-04 | 데스크톱 우선 반응형 | S |
| FR-C-05 | 결과 CSV 내보내기 (지도/상세/비교 공통) | S |
| FR-C-06 | 로그인·인증 없이 전 기능 이용 | M |
| FR-C-07 | "점포 수 감소가 폐업을 의미하지 않습니다" 안내 상시 노출 | M |

---

## 10. AI 질의응답 설계

**Tool Calling 채택.** Text-to-SQL은 LLM이 이상징후 정의를 임의 재해석할 위험이 있어 배제. 대시보드와 AI가 **동일한 분석 API**를 호출하므로 두 화면의 숫자가 항상 일치한다 — 이것이 시연의 핵심 증명 지점이다.

| 도구 | 파라미터 | 대응 질의 | 우선 |
|---|---|---|---|
| `searchAnomalyRegions` | period, catCode?, catLevel, grade?, consecutiveDecline?, sortBy?, topN | "최근 소매업이 계속 줄어든 지역은?" | M |
| `getRegionDetail` | dongCode, period | "온천2동이 왜 중점검토야?" | M |
| `compareRegions` | dongA, dongB, period, catCode? | "도안동과 온천2동 비교해줘" | M |
| `getCategoryTrend` | catCode, periodRange, scope | "대전 전체 음식업 추세는?" | S |
| `getBsiContext` | periodMonth | "요즘 대전 체감경기 어때?" | C |

`sortBy`는 `score` / `relativeGap` / `cumChange` 열거값만 허용. 임의 필드 정렬 허용 시 안전성이 무너진다.

### 처리 흐름

```
질문 → LLM(도구 선택 + 파라미터 추출)
     → 명칭 정규화 (별칭 사전: "온천2동"→행정동코드, "식당"→I2 음식)
     → 분석 API 실행
     → 결과 JSON → LLM 서술형 답변 생성
     → 답변 + 근거 표/차트 렌더링
```

### 가드레일 (필수)

- 도구 결과에 없는 수치 **생성 절대 금지** (시스템 프롬프트 명시)
- 결과 0건 → 고정 안내 응답
- 대전 외 지역 / 보유 분기 밖 → 범위 안내
- 표본 부족 조합 질의 → 판정 제외 사유 설명
- API 키는 서버 측에만 보관

### 별칭 사전 구축 필요

행정동 82개 정식명 + 축약형("온천2", "둔산3") / 업종 대분류 10 + 중분류 74 + 구어체("식당"→음식, "카페"→비알코올, "학원"→교육)

---

## 11. 코딩·문구 규칙

### 반드시 지킬 것

- **등급 명칭은 `정상 / 관심 / 주의 / 중점검토` 4개만.** '위험', '심각', '경계' 등 다른 표현 금지. 화면·API 응답·문서 전부 통일
- **'폐업률' 표현 금지.** '점포 수 변화'로 통일하고, 감소가 폐업을 의미하지 않음을 안내
- **개별 점포 폐업 추정 금지.** 집계 단위 비교만
- **AI는 도구 결과에 없는 수치를 만들지 않는다**
- 등급은 절대 기준이 아니라 **대전 내 상대 순위**임을 UI에 명시
- 모든 수치에 기준 분기 표기
- 조합 단위 수치와 행정동 단위 수치를 섞어 쓰지 않는다

### 제공하지 않는 것 (기대 관리)

개별 점포 폐업 예측 · 실시간/월 단위 갱신 · 매출·유동인구 분석 · **미래 시점 예측** · 대전 외 지역 · 지역×업종 교차 경기지수 · 계정/검토 목록 저장

> 미래 예측은 의도적으로 하지 않는다. 3개 분기로는 신뢰 구간을 확보할 수 없고, "예측이라면서 근거가 뭐냐"는 질문에 방어가 어렵다. **과거 변화 탐지에 집중한다.**

### 커밋 컨벤션

`feat:` 기능 / `fix:` 버그 / `chore:` 설정·빌드 / `docs:` 문서

---

## 12. 알려진 함정

| 함정 | 대응 |
|---|---|
| `db.migration` 단일 폴더 | 반드시 `db/migration/` 2단계. 아니면 Flyway가 **에러 없이 조용히 건너뜀** |
| 마이그레이션 파일명 언더스코어 1개 | `V1__` 2개여야 인식됨 |
| 적용된 마이그레이션 수정 | 체크섬 불일치로 부팅 실패. `V4__` 신규 생성 |
| SQL 파일에 마크다운 잔재 | 첫 줄이 `CREATE TABLE`로 시작해야 함 |
| BSI CSV 인코딩 | `cp949`. utf-8로 읽으면 실패 |
| Leaflet CSS 미import | 지도가 회색 조각으로 깨짐. `main.jsx`에 `import 'leaflet/dist/leaflet.css'` |
| `.env`와 `application-local.yml` 비밀번호 불일치 | 백엔드 인증 실패 |
| DB 비밀번호 변경 시 | `docker compose down -v` 필요 (볼륨 초기 생성 시에만 적용됨) |
| IntelliJ `Build and run using` | **Gradle**로 둘 것. IntelliJ IDEA 모드에서 의존성 해석 실패 이력 |
| `out/` 폴더 | IntelliJ 컴파일 잔재. `application-local.yml` 사본 포함 — `.gitignore` 필수 |

---

## 13. 관련 문서

| 파일 | 내용 |
|---|---|
| `SOSpot_요구사항정의서.md` (v0.6) | 기능·데이터·로직 전체 명세, 확정 사항 O-1~O-15, 검증 이력 |
| `SOSpot_사용자요구사항서.md` (v1.2) | URS UR-01~23, 인수 기준, UR↔FR 추적 매트릭스 |
| `SOSpot_개발환경_세팅가이드.md` | 실행 순서판 세팅 절차, 트러블슈팅 9건, 팀원 온보딩 |
| `README.md` | 저장소 소개, 클론 후 설정, 자주 발생하는 오류 |

---

## 14. 팀

| 담당 | 영역 |
|---|---|
| 임재민 (대표) | 프론트엔드 · 시각화 · 데이터 전처리 |
| 황민솔 | 백엔드 · 지표 로직 · AI 연동 |

2인 팀. 진로 방향은 AI 백엔드.

---

## 15. 다음 액션

1. **행정동 경계 GeoJSON 확보** (행정안전부) — 8/16까지 결론. `data/geo/`에 저장. 상가정보 `행정동코드`와 직접 조인 가능해야 함
2. `pipeline/src/load.py` 작성 → CSV 3개 로드 및 행 수 검증 (78,246 / 78,607 / 80,704)
3. `dimensions.py` → `dim_dong` 82행, `dim_category` MAJOR 10 / MIDDLE 74 생성
4. `aggregate.py` → `fact_store_count` 2단 집계 적재
5. `metrics.py` → 6장 수식대로 `fact_anomaly`, `fact_dong_score` 산출. 8장 검증 쿼리로 대조
